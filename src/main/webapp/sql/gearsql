
gear테이블-------
CREATE TABLE gear (
    gear_id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '장비 PK',

    gear_quantity INT NOT NULL COMMENT '개수',
    gear_name VARCHAR(100) NOT NULL COMMENT '장비명',
    gear_description TEXT COMMENT '설명',
    gear_price INT NOT NULL COMMENT '가격',
    gear_thumbnail VARCHAR(255) COMMENT '썸네일',

    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '생성일'
) ENGINE=InnoDB;

임시 장비------
INSERT INTO gear (
    place_id,
    gear_name,
    gear_description,
    gear_price,
    gear_thumbnail
) VALUES
(1, '어쿠스틱 기타', '버스킹용 기본 어쿠스틱 기타', 15000, 'acoustic_guitar.jpg'),
(1, '일렉 기타 앰프', '야외 공연용 고출력 기타 앰프', 20000, 'guitar_amp.jpg'),
(2, '마이크 세트', '유선 마이크 + 스탠드 포함', 10000, 'mic_set.jpg'),
(2, '포터블 스피커', '야외용 블루투스 스피커', 25000, 'portable_speaker.jpg'),
(3, '전자 키보드', '61건반 전자 키보드', 30000, 'keyboard.jpg'),
(3, '카혼', '버스킹용 타악기 카혼', 12000, 'cajon.jpg'),
(1, '마이크 스탠드', '높이 조절 가능 스탠드', 5000, 'mic_stand.jpg'),
(2, '이펙터 보드', '기타 멀티 이펙터', 18000, 'effector.jpg');
SELECT gear_id, place_id, gear_name, gear_price FROM gear;

예약자 테이블--------
CREATE TABLE gear_reservation (
    reservation_id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '예약 PK',

    gear_id BIGINT NOT NULL COMMENT '장비 ID',
    member_id BIGINT NOT NULL COMMENT '회원 PK (member.id)',

    start_datetime DATETIME NOT NULL COMMENT '대여 시작',
    end_datetime DATETIME NOT NULL COMMENT '대여 종료',

    status VARCHAR(20) DEFAULT 'RESERVED' COMMENT '상태',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '생성일',

    CONSTRAINT fk_gear_reservation_gear
        FOREIGN KEY (gear_id)
        REFERENCES gear(gear_id)
        ON DELETE CASCADE,

    CONSTRAINT fk_gear_reservation_member
        FOREIGN KEY (member_id)
        REFERENCES member(id)
        ON DELETE CASCADE
) ENGINE=InnoDB;